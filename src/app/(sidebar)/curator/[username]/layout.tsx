/** Keeps the legacy curator route inside the shared sidebar while its page redirects. */

export default function CuratorRedirectLayout({ children }: { children: React.ReactNode }) {
  return children;
}
